---
title: Excel 万能公式 14 个
source: https://mp.weixin.qq.com/s/rYPIJJA_GloCKJNFXl07PA
author: 兰色幻想-赵志东
date: 2026-06-01
tags: [Excel, 公式, 办公技巧, 数据处理, auto-compiled]
category: 办公技能
status: active
archived: 2026-06-01
---





# 工作再忙，都要学会这 14 个 Excel 万能公式！

**来源**：兰色幻想-赵志东 | Excel 精英培训

---

## 1️⃣ IFERROR 错误处理

```
=IFERROR(公式, 公式返回错误值时返回的值)
```

**示例**：VLOOKUP 查找不到时显示空白

```
=IFERROR(VLOOKUP(A9,$A$1:$D$5,3,0),"")
```

---

## 2️⃣ 多条件判断公式

```
=IF(AND(条件 1,条件 2...条件 n), 同时满足返回的值, 不满足返回的值)
=IF(OR(条件 1,条件 2...条件 n), 任一满足返回的值, 不满足返回的值)
```

**示例**：金额<500 且状态为"未到期"时显示"补款"

```
=IF(AND(A2<500,B2="未到期"),"补款","")
```

---

## 3️⃣ 多条件求和/计数

```
多条件求和：=SUMPRODUCT(条件 1*条件 2*条件 3*...*数据区域)
多条件计数：=SUMPRODUCT(条件 1*条件 2*条件 3*...*1)
```

**示例**：统计 A 产品 3 月销售合计

```
=SUMPRODUCT((MONTH(A3:A9)=3)*(B3:B9="A")*C3:C9)
```

> 💡 比 SUMIFS 慢，但可处理数组和文本型数字

---

## 4️⃣ 多条件查找公式

```
=LOOKUP(1,0/((条件 1*条件 2*条件 3...)), 返回值区域)
```

**示例**：根据姓名和工号查找职位

```
=LOOKUP(1,0/(B2:B6=B9)*(A2:A6=C9),E2:E6)
```

---

## 5️⃣ 从字符串前面提取数字

```
=-LOOKUP(,-LEFT(字符串,ROW($1:足够大的数字)))
```

---

## 6️⃣ 从字符串尾部提取数字

```
=-LOOKUP(,-RIGHT(字符串,ROW($1:足够大的数字)))
```

---

## 7️⃣ 提取任意位置字符串的数字

```
=LOOKUP(9^9,MID(数字,MATCH(1,MID(数字,ROW(1:99),1)^0,0),ROW(1:99))*1)
```

> ⚠️ 数组公式，需按 **Ctrl+Shift+Enter** 三键输入

---

## 8️⃣ 截取字符串中任一段

```
=TRIM(MID(SUBSTITUTE(字符串,"分隔符",REPT(" ",99)),(N-1)*99,99))
```

**示例**：从"-"分隔的字符串中截取第 2 个值

```
=TRIM(MID(SUBSTITUTE($A2,"-",REPT(" ",99)),(2-1)*99,99))
```

---

## 9️⃣ 分离字母和汉字

```
汉字在前：=LEFT(单元格, LENB(单元格)-LEN(单元格))
汉字在后：=RIGHT(单元格, LENB(单元格)-LEN(单元格))
```

---

## 🔟 单列不重复个数

```
=SUMPRODUCT(1/COUNTIF(区域,区域))
```

**示例**：统计 B 列客户数量

```
=SUMPRODUCT(1/COUNTIF(B2:B19,B2:B19))
```

---

## 1️⃣1️⃣ 多列不重复个数

```
=SUMPRODUCT(1/COUNTIFS(区域,区域,区域 2,区域 2))
```

**示例**：统计手机型号不重复个数

```
=SUMPRODUCT(1/COUNTIFS(A2:A7,A2:A7,B2:B7,B2:B7))
```

---

## 1️⃣2️⃣ 有条件计数不重复个数

```
=SUMPRODUCT(条件/COUNTIF(区域,区域))
```

**示例**：统计每个品牌的不重复型号个数

```
=SUMPRODUCT((A$2:A7=D2)/COUNTIFS(B$2:B7,B$2:B7))
```

---

## 1️⃣3️⃣ 多工作表同一位置求和

```
=SUM(开始工作表:结束工作表!单元格)
```

**示例**：Sheet1 到 Sheet100 的 A1 单元格求和

```
=SUM(Sheet1:Sheet100!A1)
```

---

## 1️⃣4️⃣ 金额大写转换

```
=TEXT(LEFT(RMB(单元格),LEN(RMB(单元格))-3),"[>0][dbnum2]G/通用格式元;[<0]负[dbnum2]G/通用格式元;;") 
& TEXT(RIGHT(RMB(单元格),2),"[dbnum2]0角0分;;整")
```

---

## 💡 学习建议

> 经常有同学问怎么才能快速学好 Excel 公式，兰色总是给出同样的 4 字答案：**多看、多练**。

---

## 📚 相关资源

- 兰色函数公式大全教程：144 集（88 个常用函数 + 50 多个实例）
- 四合一大全套教程：图表 209 集 + 函数 144 集 + 透视表 50 集 + 技巧大全 119 集
