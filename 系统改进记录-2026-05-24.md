# 系统改进记录 — 2026-05-24

> 触发源：《别乱装 Skill：Hermes Agent 新手的第一套能力闭环》公众号文章
> 关联原始文件：`raw/别乱装Skill-Hermes新手第一套能力闭环-微信公众号2026-05-24.md`

---

## 改进 1：修复 feishu MCP server 路径

- **问题**：`command: python3` 使用相对路径，Hermes 内部 venv 的 python3 与系统级不一致
- **修复**：改为 `/usr/bin/python3` 绝对路径
- **文件**：`~/.hermes/config.yaml` mcp_servers.feishu.command

## 改进 2：扩展 MCP 服务器（5→5，但修复了连接问题）

| Server | 修复前 | 修复后 |
|--------|--------|--------|
| feishu | `python3`（相对路径） | `/usr/bin/python3`（绝对路径） |
| ddg-search | `duckduckgo-search-mcp-server`（不存在的包） | `ddg-mcp-search`（正确包名） |
| filesystem | ❌ 不存在 | ✅ `@modelcontextprotocol/server-filesystem`（新增） |
| github | `@modelcontextprotocol/server-github`（已废弃） | `github-mcp-server`（活跃维护） |
| notion | ❌ 不存在 | ✅ `@notionhq/notion-mcp-server`（新增） |

## 改进 3：Web 后端统一指向 Camoufox

- 之前：`web: { backend: "", search_backend: "", extract_backend: "" }`（全部为空）
- 修复后：三个字段均指向 `camoufox`

## 审计结论

- 工程闭环 7 个 Skill（plan / native-mcp / github-pr-workflow / systematic-debugging / TDD / 研究类 / humanizer）：✅ 全部已安装
- 办公能力闭环 14 个 Skill（公文/文印/新媒体/知识库）：✅ 全部就位
- 剩余缺口：GitHub MCP 和 Notion MCP 需要配置对应 API Token 才能完全启用
